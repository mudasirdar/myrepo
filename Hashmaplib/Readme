#Import this library by putting folder in python/lib/dist-packages 
from Hashmaplib import *

#Inserting the first element:
root = None
root = HashMapModification.insert(root, firstnum)

#you can carry on Insert operation by passing the number as:

root = HashMapModification.insert(root, num)


#Displaying the List
HashMapModification.inorder(root)

#Deleting the number/key
HashMapModification.deleteNode(root, key):


#display the List
HashMapModification.inorder(root)


To Lookup/Search for particlar number:
  HashMapModification.Lookup(root, key):
  
i=HashMapModification.Lookup(root,key) 
print('key',i)


